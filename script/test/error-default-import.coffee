# DOCUMENTATION ONLY - error fixture (e2e skips `error-*`)
# Default import from a module without `export default` must fail.
# Covered by tests/errors.test.ts.

import Kk from './includes/only-class'
