## 任务

修复仓库审查中发现的问题，并顺手清理相关坏味道。

## 能力评估

- 本轮问题集中在本地 TypeScript/测试/文档，现有工具足够处理。
- 未发现需要额外 skill 生命周期操作的缺口。

## 计划

1. 检查模块导入链和数据模块协议，修复 `.json/.yaml` 默认导入与具名导入行为。
2. 补齐数据模块测试样例，降低仅靠快照通过的风险。
3. 修正测试报告中的误导性 coverage 说法，替换为更诚实的 reachability 报告。
4. 清理构建与输出坏味道，包括 build 的安装副作用、错误提示与版本元数据。
5. 修正文档示例并做一次完整构建测试验证。

## 风险

- 推测，待确认：YAML 由 `fire-keeper` 读取时已完成解析；若底层行为变化，需要单独引入 YAML parser。
- 数据模块若暴露顶层 `default` 键，会与默认导入槽位冲突；当前以默认导入协议优先。

## 当前状态

- 已完成：步骤 1-5
- 验证通过：`pnpm build && pnpm test`
